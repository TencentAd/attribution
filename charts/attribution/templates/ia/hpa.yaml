{{- if .Values.ia.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "attribution.fullname" . }}
  labels:
    {{- include "attribution.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "attribution.fullname" . }}
  minReplicas: {{ .Values.ia.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ia.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.ia.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.ia.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ia.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.ia.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
