{{- if .Values.impression.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "attribution.fullname" . }}
  labels:
    {{- include "attribution.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "attribution.fullname" . }}
  minReplicas: {{ .Values.impression.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.impression.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.impression.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.impression.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.impression.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.impression.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
