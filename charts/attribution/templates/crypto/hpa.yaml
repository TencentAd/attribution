{{- if .Values.crypto.enabled -}}
{{- if .Values.crypto.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "attribution.crypto.fullname" . }}
  labels:
    {{- include "attribution.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "attribution.crypto.fullname" . }}
  minReplicas: {{ .Values.crypto.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.crypto.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.crypto.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.crypto.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.crypto.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.crypto.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
{{- end }}
