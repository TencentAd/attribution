package com.tencent.attribution.index

import com.tencent.attribution.proto.click.Click.ClickLog
import com.tencent.attribution.proto.user.User.IdType
import org.scalatest.funsuite.AnyFunSuite

class RedisClickIndexTest extends AnyFunSuite {
  test("redis index") {
//    val index = new RedisClickIndex()
//    val log = ClickLog.newBuilder().setAppId("test_app_id").build()
//    index.Set(IdType.Device, "12345", log)
//    val log2 = index.Get(IdType.Device, "12345")
//    assertResult(log.getAppId, "") {
//      log2.map(c => c.getAppId).get
//    }
  }
}
